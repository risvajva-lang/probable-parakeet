# 🎬 قالب نافذة السينما VIP لووردبريس (Cinema Window WordPress Theme v2.0)

قالب سينمائي فائق السرعة والأداء، خفيف، مبني بأحدث معايير وتطويرات الويب (React 18 + Tailwind CSS + PHP / WordPress Core API) ومزود بمحرك مزامنة محتوى حقيقي متقدم مع TMDb و 36 سيرفر بث عالمي بدون إعلانات.

---

## ✨ المميزات الرئيسية للمشروع والقالب (v2.0)

1. **محرك المزامنة التلقائي الذكي (Automated Sync Engine)**:
   - فحص وجلب تلقائي للأفلام والمسلسلات والأنمي مع الحلقات والمواسم الجديدة مباشرة من TMDb إلى قاعدة بيانات ووردبريس (`cw_movie`, `cw_tv`, `cw_anime`, `cw_episode`).
   - نظام بصمة البيانات (Change Hash Signature) لمنع تحديث المنشورات وتزييف تاريخ `post_modified` ما لم تطرأ تغييرات حقيقية على المحتوى.
   - تشغيل مجدول عبر WP-Cron كل ساعتين أو عبر Cron خارجي باستخدام المفتاح الأمني `X-CW-Sync-Key`.
   - قفل أمان (Sync Lock) لمنع تداخل عمليات المزامنة المتزامنة.

2. **دعم كامل لـ 36 سيرفر بث مباشر فائق السرعة**:
   - تشغيل فوري بدون إعلانات مزعجة وبجودة تصل إلى 4K Ultra HD و 1080p FHD.
   - دعم التبديل التلقائي عند انقطاع أي سيرفر مع خيارات سيرفرات احتياطية متعددة.

3. **لوحة تحكم إدارية متقدمة وتشخيص البيانات (Admin VIP Dashboard)**:
   - أزرار تشغيل المزامنة الفورية (الكل، الأفلام، المسلسلات، الأنمي) مع شريط تقدم مباشر بالـ AJAX.
   - فحص سلامة قاعدة البيانات وتشخيص التكرارات والحلقات اليتيمة مع إمكانية التنظيف الفوري.
   - جدول سجلات المزامنة التفصيلي (المدة، العناصر المضافة، المحدثة، الحلقات، الأخطاء).
   - تخصيص قوالب عناوين الـ SEO ومفتاح TMDb API المخصص.

4. **SEO و Schema.org وتضمين واتساب الذكي (WhatsApp Rich Link Preview)**:
   - توليد مخططات JSON-LD معتمدة (`Movie`, `TVSeries`, `TVEpisode`, `BreadcrumbList`, `WebSite`).
   - وسوم Open Graph بدقة عالية وصور خلفيات وبوسترات فورية (`og:image`, `og:image:secure_url`, `og:description`, `og:type`).
   - تضمين مخصص للواتساب: عند إرسال أي رابط فيلم أو مسلسل أو حلقة في واتساب يظهر بوستر العمل بدقة فائقة مع القصة ورقم الموسم والحلقة تلقائياً.
   - دعم معايير الـ Canonical URLs ودعم اللغات المتعددة و `hreflang` / `x-default`.

5. **نظام الروابط اللاتينية النظيفة والتحويل الدائم (301 Permanent Migration Engine)**:
   - فرض الروابط اللاتينية الإنجليزية (ASCII Slugs) في كامل أجزاء الموقع ومنع الرموز والأحرف المشفرة `%D8%A7...`.
   - محرك تحويل تلقائي دائم (301 Permanent Redirect) لأي روابط عربية قديمة للمحافظة على الأرشفة وتوافق المشاركة في برامج التواصل.
   - قواميس ترجمة صوتية ذكية (Phonetic Transliteration) للعناوين وأرقام الحلقات والمواسم.

5. **خرائط الموقع الديناميكية (Dynamic XML Sitemaps)**:
   - خرائط موقع تُبنى لحظياً من سجلات قاعدة البيانات الحقيقية:
     - `/sitemap-movies.xml`
     - `/sitemap-tv.xml`
     - `/sitemap-anime.xml`
     - `/sitemap-episodes.xml`
     - `/sitemap-pages.xml`
     - `/sitemap_index.xml`

6. **REST API حديث لمزامنة المستخدم وتصفح الحلقات**:
   - نقاط نهاية لجلب الوسائط والحلقات مع روابط التنقل (الحلقة السابقة / التالية).
   - مزامنة المفضلة وسجل المشاهدة لحساب المستخدم في ووردبريس.

7. **نظام الروابط الموحدة النظيفة والهرمية (Clean Semantic URLs)**:
   - أفلام: `/movie/{slug}` مثل `/movie/inception` أو `/movie/the-odyssey-1368337`
   - مسلسلات: `/tv/{slug}` مثل `/tv/game-of-thrones`
   - حلقات مسلسلات: `/tv/{slug}/season-{s}/episode-{e}` مثل `/tv/game-of-thrones/season-1/episode-1`
   - أنمي: `/anime/{slug}` مثل `/anime/naruto`
   - حلقات أنمي مع مواسم مخصصة أو أركات: `/anime/{slug}/{season-or-arc}/episode-{e}` مثل `/anime/naruto/shippuden/episode-100`
   - Slugs نظيفة 100% بأحرف إنجليزية/لاتينية واضحة خالية من أي تشفير `%D8%...` عند المشاركة والنسخ.
   - دعم كامل للـ Web Share API للمشاركة عبر WhatsApp و Telegram و Facebook و X.

---

## 🚀 طريقة التثبيت والتشغيل في ووردبريس (Installation Guide)

### الخطوة 1: رفع مجلد القالب
قم بنسخ أو رفع مجلد `cinema-window` بالكامل إلى المسار التالي داخل استضافة ووردبريس:
```text
wp-content/themes/cinema-window/
```

### الخطوة 2: تفعيل القالب
1. ادخل إلى لوحة تحكم ووردبريس `wp-admin`.
2. اذهب إلى **المظهر (Appearance)** > **القوالب (Themes)**.
3. ستجد قالب **Cinema Window - نافذة السينما VIP**، اضغط على **تفعيل (Activate)**.

### الخطوة 3: تحديث الروابط الدائمة (Permalinks)
1. من لوحة التحكم، اذهب إلى **الإعدادات (Settings)** > **الروابط الدائمة (Permalinks)**.
2. اختر **اسم المقالة (Post name)**.
3. اضغط على **حفظ التغييرات (Save Changes)** لتفعيل الروابط النظيفة للأفلام والمسلسلات والحلقات وخرائط السايتماب.

### الخطوة 4: تشغيل المزامنة الأولية
1. من القائمة الجانبية في ووردبريس، اضغط على **نافذة السينما VIP**.
2. اضغط على **⚡ مزامنة الكل الآن** لجلب المحتوى والحلقات فورياً.

---

## 🛠️ تعديل وتطوير الكود المصدري (Development)

إذا قمت بتعديل الواجهة وأردت إعادة بناء ملفات الـ Frontend المجمعة:
```bash
npm run build
```
سيتم تحديث الملفات المجمعة تلقائياً في `cinema-window/assets/`.

---

© جميع الحقوق محفوظة - فريق تطوير نافذة السينما VIP.
